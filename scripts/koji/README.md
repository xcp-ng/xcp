# Koji tools

## Dependencies

Run

~~~sh
pip install --user -r requirements.txt
~~~

to install the required dependencies.

You also need to install RPM python bindings, for instance on Debian based systems:

~~~sh
sudo apt install python3-rpm
~~~
